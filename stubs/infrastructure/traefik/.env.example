# Traefik Global Proxy Configuration for tuti-cli
# ================================================

# Timezone
TZ=UTC

# Log level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# Domain for local development
# All projects will use subdomains like: myapp.local.test
DOMAIN=local.test

# Let's Encrypt Email (for production use)
ACME_EMAIL=admin@local.test

# Network name (should match traefik_proxy)
TRAEFIK_NETWORK=traefik_proxy
