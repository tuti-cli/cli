# ============================================================================
# 🐳 TUTI-CLI DEVELOPMENT ENVIRONMENT - WORDPRESS
# ============================================================================
# This file contains ONLY tuti-cli specific variables and Docker service
# overrides. These values are used by Docker Compose for container setup.
# ============================================================================

# ----------------------------------------------------------------------------
# Project Configuration
# ----------------------------------------------------------------------------
PROJECT_NAME=wordpress
APP_ENV=dev
APP_DOMAIN=wordpress.local.test

# ----------------------------------------------------------------------------
# Installation Type
# ----------------------------------------------------------------------------
# Options: standard | bedrock
WP_INSTALLATION_TYPE=standard

# ----------------------------------------------------------------------------
# Docker Build Configuration
# ----------------------------------------------------------------------------
PHP_VERSION=8.3

# Docker User/Group IDs (for Linux file permissions)
# Set these to match your host user: `id -u` and `id -g`
DOCKER_USER_ID=1000
DOCKER_GROUP_ID=1000

# ----------------------------------------------------------------------------
# WordPress Configuration
# ----------------------------------------------------------------------------
WP_TABLE_PREFIX=wp_

# WordPress Debug Settings (development only)
WP_DEBUG=true
WP_DEBUG_LOG=true
WP_DEBUG_DISPLAY=true

# ----------------------------------------------------------------------------
# Database Configuration (MariaDB or MySQL - selected during install)
# ----------------------------------------------------------------------------
DB_HOST=database
DB_PORT=3306
DB_DATABASE=wordpress
DB_USERNAME=wordpress
DB_PASSWORD=secret

# MariaDB settings (if using MariaDB)
MARIADB_VERSION=11.4
MARIADB_ROOT_PASSWORD=rootsecret

# MySQL settings (if using MySQL)
# MYSQL_VERSION=8.4
# MYSQL_ROOT_PASSWORD=rootsecret

# External Port Mappings (for connecting from host machine)
DB_EXTERNAL_PORT=3306

# ----------------------------------------------------------------------------
# WordPress Salt Keys
# Generate new keys at: https://api.wordpress.org/secret-key/1.1/salt/
# These will be auto-generated during installation
# ----------------------------------------------------------------------------
WP_AUTH_KEY=change-me-to-a-unique-phrase
WP_SECURE_AUTH_KEY=change-me-to-a-unique-phrase
WP_LOGGED_IN_KEY=change-me-to-a-unique-phrase
WP_NONCE_KEY=change-me-to-a-unique-phrase
WP_AUTH_SALT=change-me-to-a-unique-phrase
WP_SECURE_AUTH_SALT=change-me-to-a-unique-phrase
WP_LOGGED_IN_SALT=change-me-to-a-unique-phrase
WP_NONCE_SALT=change-me-to-a-unique-phrase

# ----------------------------------------------------------------------------
# Mail Configuration (Mailpit for development)
# ----------------------------------------------------------------------------
MAIL_HOST=mailpit
MAIL_PORT=1025

# ----------------------------------------------------------------------------
# Redis Cache (Optional - uncomment if using Redis)
# ----------------------------------------------------------------------------
# REDIS_HOST=redis
# REDIS_PORT=6379
# WP_REDIS_HOST=redis
# WP_REDIS_PORT=6379
