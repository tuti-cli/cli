Write comprehensive Pest tests for Tuti CLI: $ARGUMENTS

Use the `write-tests` skill which contains:
- Complete Pest templates for commands and services
- Workflow: Analyze → Determine Type → Write by Category → Mock Dependencies
- Process::fake() patterns for Docker commands
- Service mocking strategies
- File naming conventions
