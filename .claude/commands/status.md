# status
Live project dashboard.

**Usage:** `/status`

Invoke `tuti-workflow-master`:
> "Show project status dashboard: milestone progress (open/closed per phase), issues in-progress with worktree paths, issues in-review with PR links, needs-confirmation count (triage backlog), next 5 ready issues, blocked issues with reasons, active git worktrees mapped to issue numbers."
