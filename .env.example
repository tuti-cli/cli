APP_NAME="TUTI"

# Docker Configuration
# User ID Configuration (match your host user)
# Run: id -u (for PUID) and id -g (for PGID) on your host
PUID=1000
PGID=1000
USER=youruser

# Application
APP_ENV=local
APP_DEBUG=true

# PHP Configuration
PHP_MEMORY_LIMIT=512M
PHP_MAX_EXECUTION_TIME=120

# Custom Keys (add your own as needed)
# SECRET_KEY=
# API_KEY=
